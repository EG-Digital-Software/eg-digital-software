# Loaded only by `vite --mode tunnel` (npm run dev:tunnel). A relative API base
# keeps calls same-origin so they flow through the Vite /api proxy to the local
# backend — no CORS, and the public tunnel URL just works from any device.
VITE_API_URL=/api
