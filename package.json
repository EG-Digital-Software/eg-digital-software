{
  "name": "eg-digital",
  "version": "1.0.0",
  "private": true,
  "description": "EG Digital SaaS Platform — monorepo (frontend + backend)",
  "workspaces": ["frontend", "backend"],
  "scripts": {
    "dev": "npm run dev --workspace backend & npm run dev --workspace frontend",
    "dev:backend": "npm run dev --workspace backend",
    "dev:frontend": "npm run dev --workspace frontend",
    "dev:tunnel": "npm run dev:tunnel --workspace frontend",
    "tunnel": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/tunnel.ps1",
    "build": "npm run build --workspace backend && npm run build --workspace frontend",
    "seed": "npm run seed --workspace backend",
    "migrate": "npm run prisma:migrate --workspace backend",
    "prisma:studio": "npm run prisma:studio --workspace backend"
  }
}
